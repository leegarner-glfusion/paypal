{# begin {templatelocation} #}
<form action="{action_url}" method="post">
  <input type="hidden" name="id" value="{id}">
  <input type="submit" class="paypalButton orange" border="0" 
    name="download" value="{$LANG_PP['download']}" />
</form>
{# end {templatelocation} #}
